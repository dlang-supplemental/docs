= Tagged proposal catalog
:date: 2026-08-15

* Tagged xref:blog/thread-local-gc-tgc.adoc[Thread-local garbage collection (`tgc`) for D] as a `proposal`.
* Added a Proposals section on the portal home page that loads articles from `page-catalog.json` (built by `lib/page-catalog-extension.js` from `:page-tags:`).
