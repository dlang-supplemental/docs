= 2026-03-05 – Portal Genesis and CI

* Bootstrapped the D Lang Supplemental Antora hub repository integrating the `antora-dark-theme` UI bundle.
* Resolved configuration bugs across internal component repositories (`densor`, `fluidsync-daemon`, `vector-store-dlang`, `vibe-search-core`, `asciidoctor-dlang`) by explicitly enforcing the `start_path` routing to `/docs` instead of repo roots.
* Stabilized GitHub fetching logic by targeting the `[main]` branch exclusively instead of attempting to merge `[master, main]`.
* Added GitHub Actions continuous deployment workflows and locked PNPM cache states.
