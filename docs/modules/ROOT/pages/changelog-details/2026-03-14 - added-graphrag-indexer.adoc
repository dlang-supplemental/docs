= 2026-03-14 – Added GraphRAG indexer to documentation playbooks

* Added the newly created `graphrag-indexer` documentation site module to both local (`antora-playbook-local.yml`) and remote (`antora-playbook.yml`) Antora playbooks.
* Initialized central changelog for the `dlang-supplemental` documentation repository to track cross-project docs updates.
